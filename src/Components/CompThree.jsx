import React from 'react';

const CompThree = ({ name, id }) => {
    return (
        <div>
            <h1>{name}</h1>
            <h1>id: {id}</h1>
        </div>
    )
}

export default CompThree;